# Project Memory — sigma-vite

A scratchpad for project-specific learnings, decisions, and gotchas. **Distinct from global auto-memory:**

- **Global auto-memory** — `~/.claude/projects/-Users-cj0yn3r/memory/`. Cross-project preferences (terseness, git confirmation, etc.). Auto-managed.
- **This file** — facts that only matter inside sigma-vite. Manually curated. Committed to the repo so future sessions (and other agents) inherit it.

## When to update this file
Add an entry when you learn:
- A non-obvious architectural decision
- A workaround for an upstream bug
- A convention that doesn't fit cleanly in `.claude/rules/`
- Something the next session shouldn't have to rediscover

## Architectural Decisions
_(none yet)_

## Gotchas
_(none yet — populate as discovered)_

## Upstream Sync Log
_(record `git fetch upstream && git merge upstream/main` events with date + commit range, so regressions are easier to spot)_

| Date | Upstream HEAD before | Upstream HEAD after | Notes |
|------|---------------------|---------------------|-------|
| 2026-05-08 | — | `1306a5d` | Initial fork |
